import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.michael.assessment',
  appName: 'MoviePro Reviews',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
